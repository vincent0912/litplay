{
  "name": "start-lit-element",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "npm-run-all --parallel tsc-watch serve:dev",
    "tsc-watch": "tsc --watch",
    "serve": "polymer serve",
    "serve:dev": "polymer serve --proxy-target http://localhost:3000/api --proxy-path api",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "rollup -c"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "lit-element": "^2.0.1"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5",
    "rollup": "^1.6.0",
    "rollup-plugin-node-resolve": "^4.0.1"
  }
}
